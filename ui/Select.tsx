// const Select = ({ item, setValue }) => {

//   return (
//     <select
//       className="bg-slate-900 p-3 rounded-md min-w-40"
//       onChange={(e) => setValue(e.target.value)}
//     >
//       {item.map((e, i) => (
//         <option value={e.value} key={i}>
//           {e.option}
//         </option>
//       ))}
//     </select>
//   );
// };

// export default Select;

import React from "react";

const Select = () => {
  return <div>Select</div>;
};

export default Select;
